import subprocess

try:
    print('try')
    #raise ValueError('Invalid ERROR: ')
    #assert 'stage' is 's'
    #result = subprocess.run(['docker', 'rmi', 'docker-test-app'], stderr=subprocess.PIPE)
    #result = subprocess.run(['docker', 'images'], stderr=subprocess.PIPE)
    #error = result.stderr
    ##if len(error.decode("utf-8")) is 0:
    #    print('empyl')
    e = 'dff'
except ValueError as e:
    print(e)


print(e)
